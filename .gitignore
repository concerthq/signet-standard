node_modules/
*.log
.DS_Store
_site/

# Local delivery/handoff packs — not part of the standard.
handoffs/

# Agent demo output is generated at runtime.
agent/output/
onboarding/output/
auction/output/

# Conformance reports are generated; the committed samples show pass/fail.
conformance/reports/*-report.json
!conformance/reports/reference-adapter-report.json
!conformance/reports/broken-adapter-report.json
!conformance/reports/reference-adapter-endorsement-report.json
!conformance/reports/broken-adapter-endorsement-report.json

# local plaintext naming deny-list — never committed
.naming-denylist
**/.naming-denylist

# Repository inventory is generated on demand and attached to CI runs as an artifact.
signet-inventory.json
signet-inventory.*.json
