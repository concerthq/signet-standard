# SIGNET defect register

**Status:** Tier 1 governance record · **Asserts facts, adopts nothing**
**Baseline:** v0.15.0 · **Supersedes:** the defect table in `governance/WITHDRAWAL-2026-08.md`

A defect is a fact about a current artifact, verifiable against the tree. It carries no gate and
needs no resolution to be recorded. Remedies are separate; a defect may sit open indefinitely
without prejudice.

Promoted out of the withdrawal record so that record stops accreting and this becomes the single
index.

| # | Defect | Verifiable at | State |
|---|---|---|---|
| D-1 | `Mandate` asserts a lifecycle it can record on no surface | `schema/mandate.schema.json`, `codelists/eventTypeCore.csv` | **Closed** — v0.15.0 registry + S-1..S-3 |
| D-2 | No artifact maps events to state changes; §1.7 and GRT-1 not executable | absence | **Closed** — v0.15.0 registry |
| D-3 | `sourcingEvent.published` names a state absent from the enum | `codelists/eventType.csv` | **Closed** — rebound to `planned → active` |
| D-4 | No rule states which surface governs where stored `status` and the event stream disagree | `governance/proposals/README.md` | **Closed** — S-2, S-3 |
| D-5 | `Bid.status` carries `superseded`, a relation, which no event can project | `schema/bid.schema.json` | **Closed** — IAR-0002 |
| D-6 | `eventType` has nine codes for eighteen objects; nothing past `contract.signed` | `codelists/eventType.csv` | Open |
| D-7 | `Mandate.scope` is required and unconstrained; `{}` satisfies it | `schema/mandate.schema.json` | Open |
| D-8 | A relying party without stream access cannot determine whether a mandate is effective | `schema/approval.schema.json` vs `schema/mandate.schema.json` | Open |
| D-9 | Erasure versus the hash chain is unaddressed outside the `identity` extension's person-reference rule | `conformance/levels.md`, `docs/extensions/identity.md` | **In progress** — generalising the no-PII rule to core |
| D-10 | No object carries a supersession reference; only `Policy` carries `version`. Subject identity across supersession decides whether the per-subject chain continues or forks | 29 schemas | Open |
| D-11 | No distinction between an actor entitled to request a transition and one entitled to effect it; `Mandate.grantedBy` is singular with no delegation reference | `schema/mandate.schema.json`, `schema/event.schema.json` | Open — request/perform partly answered by the registry |
| D-12 | Every inter-object reference is an untyped `Identifier`. `Contract.award` may reference a `Party` and pass C-DOC | `schema/definitions.schema.json` | Open |
| D-13 | `codelists/submissionStatus.csv` duplicates the inline enum on `Submission.status` and is referenced by no schema — two records of one relationship | `codelists/submissionStatus.csv` | **Closed** — CSV deleted; the inline enum is the single record (IAR-0003) |
| D-14 | Five closed codelists were `"type": "string"` with the CSV named only in a description; `{"procedure": "banana"}` passed document conformance | `schema/sourcing-event.schema.json` and four others | **Reopened** — was recorded Closed under IAR-0003. The binding manifest and the checker landed at v0.16.0; the generated enums did not, and IAR-0003 has never been opened. Four bindings fail `check-codelist-binding.js` and `identifierScheme` is deferred pending D-20, so all five remain unenforced and any string still validates. Closes when IAR-0003 lands (D-30) |
| D-15 | The transition registry declared 9 of 29 objects while `docs/state-model.md` L-2 requires every object declared. Statelessness by omission is the defect L-2 exists to prevent, reproduced in the artifact that defines it | `state-model/state-model.json` | **Closed** — 29 declared; CI check C12 (cited at v0.16.0, landed #57 — D-53) |
| D-16 | Two core edges rested on a jurisdictional basis with no corroborating general source, which B-1 permitted because it asked only whether a source was external | `state-model/state-model.json` | **Closed** — B-3, CI check C14 (cited at v0.16.0, landed #57 — D-53) |
| D-17 | `conformance/levels.md` GRT-1 is exercised only by endorsement checks not in force | `conformance/levels.md` | Open — recorded by the standard itself |

| D-18 | State vocabularies exist twice — as a schema `enum` and as registry `states` — and CI asserts agreement rather than generating one from the other. `docs/state-model.md` §11 D-1 defines a second hand-maintained copy as a defect, so the rule was violated by the artifact that states it | `state-model/state-model.json`, `schema/*.schema.json` | **Closed** — enum generated from the registry; registry promoted to normative (IAR-0004) |

| D-19 | The rule that Concert names no individual and no commercial implementer has existed since v0.13.0 and is enforced by nothing. The defect is the absent control. No instance of a breach is currently identified in the repository | absence — no check enforced the rule | **Closed** — `conformance/rules/check-naming.js` added and run in CI; the enforcement gap is closed. No breach is asserted |

| D-20 | `Decision.inputs` is documented to hold credentials (`docs/specification.md:409`, `wiki/Agent-Layer.md:113`) and is typed `Identifier[]`. `Identifier.scheme` is closed to six entity-naming codes, none of which names a credential. No value satisfies both, so the specification mandates content the schema cannot express. `Provenance.derivedFrom` has the same shape. `Credential.id` is a bare URI string with no documented mapping to `Identifier` | `schema/decision.schema.json`, `schema/definitions.schema.json#/definitions/Identifier`, `docs/specification.md:409,463` | Open — CDM-level. Present since v0.6.1; invisible until `Identifier.scheme` was constrained under IAR-0003. Not resolvable within IAR-0003; requires its own record and comment period |

| D-21 | Nothing checks that the schema can express what the prose requires. `docs/` and `wiki/` state field contents that the corresponding schema may forbid, and no check compares them. D-20 is one instance and was invisible for ten releases | absence — no check compares documented field contents against schema constraints | Open |

| D-22 | A drafted governance correction was superseded by other means and never closed out. Two records continued to describe it as unapplied. Nothing checks that a drafted correction is either applied or closed | `governance/site-alignment/`, `CHANGELOG.md`, `governance/README.md` | **Closed** — `OUTCOME.md` recorded; records corrected |

| D-23 | The committed Pages artifact `_site/index.html` states version 0.10.0, five releases behind `package.json`. `pages.yml` rebuilds only when `docs/specification.md`, `tools/build-pages.js` or the workflow changes, so a release alone does not regenerate it. A derived artifact that nothing regenerates on the event that invalidates it | `_site/index.html`, `.github/workflows/pages.yml` | Open |

| D-24 | Deleting `codelists/submissionStatus.csv` under IAR-0003 made `concert.foundation/signet/v0.1/codelists/submissionStatus.csv` return 404 — a published `$id`-space URL, proxied from `main` with no cache and no fallback. IAR-0003 asserted the file had no consumer; two existed in `concert-website` and neither was recorded here, so the claim was unfalsifiable from inside this repository | `governance/IAR-0003-codelist-enforcement.md:89`, `codelists/submissionStatus.csv` | **Closed** — restored retired-but-resolvable; IAR-0003 corrected |

| D-25 | `concert-website` built from this repository at `main`, unpinned — roughly eighty build-time fetches and five rewrites proxying the `signet/v0.1/` `$id` namespace. Every merge to `main` published as the standard on the next deploy, including changes still inside an open fourteen-day comment period, and any deletion broke the build cold | `concert-website` `scripts/generate-standard.mjs`, `next.config.mjs` | **Closed** — `concerthq/concert-website#12` merged (`f50ebca`). Both mechanisms pin to `v0.16.0` from one shared constant, `standard-ref.mjs`; neither declares a ref of its own |

| D-26 | Six source comments in `concert-website` and `docs/WEBSITE_BUILD_NOTE.md:17-19` stated the site fetches at a pinned tag. Both mechanisms used `main`. Three records of one decision, two of them wrong. The handoff scoping this work said five comments; there are six, and one of them — "tag below" — was inaccurate in a second way once the constant moved file | `docs/WEBSITE_BUILD_NOTE.md:17-19`, `concert-website` `scripts/generate-standard.mjs`, `next.config.mjs` | **Closed** — `concerthq/concert-website#12` merged (`f50ebca`). Code and comments agree; the one comment that was inaccurate in a second way was corrected with them |

| D-27 | The repository declares no public interface. Roughly eighty paths are consumed by an external build and five rewrites proxy published `$id` URLs, with no manifest and no check that a declared path resolves. A claim that a file has no consumer is therefore not testable from inside this repository | absence — no manifest, no check | **Closed** — `public-interface.json` declares 86 published and 26 consumed paths; `conformance/rules/check-public-interface.js` fails on any absent path and runs in CI |

| D-28 | `codelists/*.csv` admits no comment or metadata line: the workflow lint and `readCodes()` both treat line 1 as the header, so the format cannot record that a file is retired, deprecated or non-authoritative. Disposition is carried only in `codelists/bindings.json`, which a direct consumer of the CSV never sees | `.github/workflows/validate.yml` CSV header lint, `conformance/rules/check-codelists.js` | Open |

| D-29 | `governance/IAR-0005` was merged before its stated fourteen-day comment period elapsed, to restore a published `$id`-space URL returning 404. `GOVERNANCE.md` requires the period before merge for every Tier 2 change and names no urgency exception; the `WITHDRAWAL-2026-08.md` carve-out is an exception to the proposal moratorium and itself requires a stated period. So this is a departure, not an exemption. Recorded per `GOVERNANCE.md:67`. The underlying gap: the process has no route for an urgent correction, so every one becomes a recorded departure | `governance/IAR-0005-published-url-restoration.md`, `GOVERNANCE.md` | Open — closes when `GOVERNANCE.md` gains a carve-out for a defect actively causing harm, adopted under its own Tier 2 route |

| D-30 | The v0.16.0 CHANGELOG claimed closed-codelist enforcement landed. The manifest and checker landed; the generated enums are on the unopened IAR-0003 branch, so five closed codelists remain unenforced in schema and `{"procedure": "banana"}` validates on `main`. `check-codelist-binding.js` was not wired into `validate.yml`, so nothing surfaced the gap. A released record asserted an enforcement not in force. The same claim stood in `governance/defects.md` D-14, `governance/IAR-0003-codelist-enforcement.md` and `docs/state-model.md` | `CHANGELOG.md` v0.16.0, `codelists/bindings.json`, `.github/workflows/validate.yml` | Open — closes when IAR-0003 lands and the enums are generated *Partially corrected 2026-08-29: two clauses no longer hold. The checker **is** wired into `validate.yml` (landed `84d9931`; see D-42), and this record's pull request **was** opened — #43, 2026-08-21T00:26Z, since approved and still unmerged (see D-43). The enforcement gap the entry records is unaffected and stays open: the enums have not landed on `main`.* |

| D-31 | The v0.16.0 CHANGELOG states that promoting `state-model/state-model.json` to normative means the file gains `CODEOWNERS` protection. The generated lifecycle enums did land on `main`; the ownership entry did not. `/state-model/state-model.json` is absent from `.github/CODEOWNERS` on `main`, and present only on `v0.16-iar-0004-registry-normative`, which has not merged. A normative artifact is unprotected while a released entry says it is protected | `.github/CODEOWNERS`, `CHANGELOG.md` v0.16.0, `state-model/state-model.json` | Open — closes when the CODEOWNERS entry lands with IAR-0004 |
| — | *Numbering note: D-32 and D-33 are reserved by rows on unmerged branches (`v0.16-iar-0003-codelist-enforcement`, `v0.16-iar-0004-registry-normative`, and `wiki/correct-drift` respectively) while `main` stops at D-31. The numbers are taken; D-34 onward are assigned across the gap so they stay stable when those branches merge. The IAR series likewise has no IAR-0001.* | — | — |
| D-34 | No core object addresses an evaluation criterion. `Evaluation.criteria` is a single `Identifier` to a `Policy` whose `expression` is an opaque string; `Score.criterion` is `"type": "string"` with no constraint. A score cannot name the criterion that produced it, and nothing outside the `Evaluation` can point at a criterion at all | `schema/evaluation.schema.json` `/properties/criteria`, `schema/definitions.schema.json#/definitions/Score/properties/criterion` | Open — remedy drafted, unregistered (`CP-Requirement-Sockets` §2.1) |
| D-35 | `Obligation` records what discharges it (`dischargedBy`) and nothing about where it came from. The `Provenance` definition, whose `derivedFrom` expresses exactly that, is referenced by `Decision` and not by `Obligation`. The link from what was asked to what was promised is unrepresentable | `schema/obligation.schema.json` (no `provenance`), `schema/definitions.schema.json#/definitions/Provenance` | Open — remedy drafted, unregistered (`CP-Requirement-Sockets` §2.2) |
| D-36 | `Submission` has no structured surface on which a submitting party states a position on any element of what was asked. It carries `items`, `value`, `disclosedCredentials` and `sealedProof`; compliance, qualification and evidence against a requirement exist only as attached documents or prose | `schema/submission.schema.json` (absence) | Open — remedy drafted, unregistered (`CP-Requirement-Sockets` §2.3) |
| D-37 | Two core codes in a closed codelist match the CP-Extension-Composition prefix grammar. `identifierScheme.csv` carries `gleif:lei` and `gs1:gln`; the Part 1 pattern `^[a-z][a-z0-9-]*:` matches both. Under CP-EventType-Closure §2.4 — "a value MUST appear in the core codelist **or** carry a valid prefix" — generalised to any other list, a core code and a private prefixed value with the same text are indistinguishable, and `gleif:anything` passes as private. The grammar and the codelist were written without reference to each other | `codelists/identifierScheme.csv` rows 2–3, `governance/proposals/CP-Extension-Composition.md` §2 Part 1, `governance/proposals/CP-EventType-Closure.md` §2.4 | Open — input to gate E-3; the rule must reserve every prefix already used by a core code, or the value rule must apply only where no core code carries a colon |
| D-38 | Eleven tracked schemas do not carry the Part 1 `patternProperties` pattern, including every in-tree extension root object: `approval`, `auction`, `bid`, `coverage-assessment`, `coverage-policy`, `exposure-position`, `hedge-proposal`, `price-mark`, `scenario`. An implementer may carry `example-org:costCentre` on an `Order` and may not carry it on a `Bid`. CP-Extension-Composition §2.1 scoped the pattern to "all 18 root objects" and four embedded objects and did not consider extension roots; the v0.16.0 changelog records 22 sites, consistent with the CP, so this is a scope gap rather than an execution gap | inventory `schemas.files[*].patternProperties`; `CHANGELOG.md` (22 sites) | Open — input to gate E-1 |
| D-39 | IAR-0003 generates a bare `enum` at each bound property ("regenerates the enum from the CSV"). CP-EventType-Closure §2.4 requires a closed-list value to be accepted when it carries a valid prefix, and CP-Codelist-Enforcement leaves the general case open as C-4. A bare `enum` cannot admit a prefixed value. On landing as written, IAR-0003 forecloses §2.4 for `decisionType`, `documentType`, `partyRole` and `procedure`, deciding C-4 by generator shape rather than on the record. IAR-0003's pull request has not been opened (D-30), so the shape can still be decided before it is | `governance/IAR-0003-codelist-enforcement.md` "The change"; `governance/proposals/CP-EventType-Closure.md` §2.4; `governance/proposals/CP-Codelist-Enforcement.md` §7 C-4 | Open — timely. The generator emits either `enum` (C-4 resolved against extension values, and §2.4 becomes an `eventType`-only exception) or `anyOf: [{enum}, {pattern}]` (C-4 resolved for them). Either is a decision; neither should be a side effect |
| D-40 | `CHANGELOG.md` v0.16.0 states the implementer enquiry is "answered at `governance/RESPONSE-2026-08-implementer-enquiry.md`". The path is absent from `main`. A released record cites a path that does not exist — the D-31 pattern | `CHANGELOG.md` v0.16.0 line 14; `git ls-files governance/ \| grep RESPONSE` on `main` returns nothing | **Closed** — `governance/RESPONSE-2026-08-implementer-enquiry.md` landed 31 August, eleven days after the v0.16.0 changelog cited it. The response records the delay and states, per correction, what has and has not landed at v0.16.1 (the generated enums of IAR-0003 have not — see D-14, D-30). Recorded from verification candidate C-1; closed on the artifact, not on the claim |
| D-41 | `$ref` carrying sibling annotations that Draft-07 ignores is far more widespread than recorded. CP-Extension-Composition §3.1 names five affected fields, all `allOf`-wrapped `$comment` sites in `InvoiceLine` and `VatBreakdown`. Thirty-four further sites carry a bare `$ref` beside a `description`, across ten schemas, and no validator surfaces any of them. §3.1's scope is understated by an order of magnitude | `schema/*.json` — 34 `$ref`-with-`description` sites on `main`, none in `invoice.schema.json`; `governance/proposals/CP-Extension-Composition.md` §3.1 | Open — recorded from verification candidate C-2. The candidate observed 22 against project-workspace copies; the count on `main` is 34 |
| D-42 | D-30 states `check-codelist-binding.js` "was not wired into `validate.yml`, so nothing surfaced the gap". The wiring landed at `84d9931` and the checker runs in CI on `main`. D-30's text is stale on that clause while the enforcement gap it records remains open | `.github/workflows/validate.yml` line 109; `governance/defects.md` D-30 | Open — recorded from verification candidate C-3. D-30 stands; the wiring clause is the part that no longer holds |
| D-43 | Three records assert that IAR-0003's pull request has never been opened. It was opened 2026-08-21T00:26Z as #43, is not a draft, and has been approved. `governance/IAR-0003-codelist-enforcement.md` line 12 carries the claim as a status line; `CHANGELOG.md` line 97 repeats it in a released correction; the D-39 amendment as authored rests its whole premise on it ("so this decision can still be taken on the record rather than by side effect"). The structural cause is that forge state — pull requests, review decisions, draft status — is invisible to every verification route this repository has: the generated inventory covers tracked files only, so a claim about a pull request cannot be grounded the way a claim about a schema can, and all three assertions were authored from each other rather than from the forge | `governance/IAR-0003-codelist-enforcement.md:12`, `CHANGELOG.md:97`, the folded amendment's premise; `gh pr view 43` | Open — the ordering constraint it grounded is restated as *before merge*, which still holds. Corrections are dated notes, not silent edits. Closes when either the inventory extractor gains a forge section (`gh api`: open pull requests, review decisions, draft state) or PR-state assertions are forbidden in records. *Third instance, 2026-08-29: #44's body says "Stacked on #40" and directs "Merge #40 first". #40 was closed 2026-08-21T00:21:51Z, four minutes before #43 opened, and was superseded by it. Same class, same cause — a forge reference authored once and never re-verified, in a record nothing can check. The pattern is now three for three, which is the argument for the forge section rather than for a discipline of re-reading.* |

| D-44 | `conformance/certification.md` has specified a public registry since it was written — §2 step 4 records a certification "in the public registry", §3 makes "the registry record the source of truth" and licenses a short-form mark only against "a resolvable link to the registry entry", and §3.1 titles itself "Registry entries" and lists their fields. No such artifact existed. `conformance/registry.md`, `conformance/registry.json` and `registry/README.md` are all absent from `main`, so a mark string had nothing to resolve to and §3's resolvability condition was unsatisfiable by construction. The claim is not confined to this repository: `docs/profiles/auction-platform.md` states that a certified platform's "registry entry records that the `auction-platform` profile was assessed", and the published post `concert-website content/posts/auction-platform-certification-open.mdx` promises implementers "a public registry listing". `certification.md` is served at `https://concert.foundation/signet/v0.1/conformance/certification.md`, so the specification of the missing artifact is itself published. **This is the third recorded instance of one pattern and the family is growing:** D-31 (a released changelog entry states `CODEOWNERS` protection that never landed), D-40 (a released changelog entry cites `governance/RESPONSE-2026-08-implementer-enquiry.md`, absent from `main`), and now D-44 — with D-24 the same shape arrived at by deletion rather than by never existing. Three of the four were found by reading records against the tree by hand, one at a time; nothing checks that a path or artifact named in a normative or released record exists, which is why the count keeps rising rather than the cause being closed | `conformance/certification.md` §2 step 4, §3, §3.1; `docs/profiles/auction-platform.md`; `concert-website content/posts/auction-platform-certification-open.mdx:23`; absence of `conformance/registry.*` and `registry/` on `main` | **Closed** — `conformance/certification-register.md` created in this change: closed, append-only, zero entries, stating that emptiness is the accurate state and that no certification has been issued. Indexed from `governance/README.md`, linked from `certification.md` §2/§3/§3.1, and declared `published` in `public-interface.json` so `check-public-interface.js` fails if it is ever removed. *Residue, stated rather than left to be discovered: `concert-website` pins this repository at a release tag, so the published `certification.md` continues to name a registry that returns 404 at `/signet/v0.1/conformance/certification-register.md` until that pin advances past the tag carrying this change. The artifact exists; the published URL follows the pin (D-25).* **The family is not closed by this row.** The absent control is a check that every artifact named in a record exists — the register-side counterpart of `check-public-interface.js`, which today tests only paths declared in the manifest, not paths asserted in prose. D-21 records the adjacent absent control for schema-versus-prose; this is the same gap for record-versus-tree |

| D-33 | The wiki is a third record of the schema and of the process rules, hand-maintained, and has drifted on both. `wiki/Process-Layer.md:350` documented `Bid.status` with `superseded`, removed from the schema at v0.15.0 under IAR-0002 and used in two implementer-facing documents as the example of what not to model as a state. `wiki/Codelists.md:9`, `wiki/Codelists.md:133`, `wiki/Contributing.md:29`, `wiki/Governance-and-Versioning.md:18` and `wiki/Extensions.md:234` asserted a Standards Committee revision process that the v0.16.0 reconciliation corrected elsewhere — `Governance-and-Versioning.md` contradicted itself fourteen lines apart — and the changelog recorded the sweep as complete. Nothing compares documented enums against schema enums, or documented process against `GOVERNANCE.md`. | `wiki/Process-Layer.md`, `wiki/Codelists.md`, `wiki/Contributing.md`, `wiki/Governance-and-Versioning.md`, `wiki/Extensions.md` | Open — the prose is corrected here; closes when a cross-reference check is in force, **covering version and status claims and not only enums and process** (see the note below). *Version claims, 2026-08-30.* `wiki/Home.md:93-94`, `wiki/FAQ.md:105` and `wiki/Governance-and-Versioning.md:81-82` carried "v0.1 (Working Draft / Request for Comments)" and "not yet frozen" — the same self-description `docs/specification.md` carried until it was corrected in this change — and two of them stated the repository at **v0.10.0**. All are corrected here by removing the restated number rather than re-pinning it. **The asymmetry is the finding.** The v0.16.0 reconciliation corrected the Standards Committee clause in *both* records: `docs/specification.md` §12.2 and `wiki/Governance-and-Versioning.md:17` each say plainly that no Committee is constituted, and line 17 is the correction this row was opened for. The **version** claim was corrected in *neither* — it survived simultaneously in the spec header and on three wiki surfaces. So the sweep discipline operates **per topic, not per record**: a reconciliation fixes the clause it was called for wherever that clause appears, and leaves every other stale clause in the same paragraph standing. A cross-reference check that compares documented enums against schema enums and documented process against `GOVERNANCE.md` would have caught neither of these, which is why this row's closure condition is widened above. *One origin event, not four.* `wiki/Home.md`, `wiki/FAQ.md` and the release-history table in `wiki/Governance-and-Versioning.md` — which simply had no row past it — were frozen at exactly **0.10.0**, and `_site/index.html` states the same 0.10.0 under **D-23**. Four surfaces at one value is not four independent lapses: it dates the last sweep of these records, and says the remedy is regeneration rather than editing. D-23 gives the mechanism on the `_site` side — `pages.yml` rebuilds only on a change to the spec, the build tool or the workflow, so a release alone never regenerates it. The wiki has the same property and no trigger at all. The release-history table is **cut**, not backfilled: it was a second hand-maintained copy of `CHANGELOG.md`, which is the file class this workstream removes, and six release summaries is authoring rather than a correction. The section now points at the changelog as the single release record, and this note is where the drift itself is preserved |
| D-45 | `docs/extensions/commodity-risk.md` §5 specifies four additions to core objects under a namespace key `commodityRisk` — `Contract.commodityRisk.positions[]`, `Need.commodityRisk.sourceProposal`, `Award.commodityRisk.tranche`, `Decision.commodityRisk.assessmentContext` — and the merge record (`governance/reviews/2026-07-commodity-risk-merge.md` §1.3) confirms them as satisfied. No core schema carries a `commodityRisk` property, and the extension socket that did land (CP-Extension-Composition Part 1, the `^[a-z][a-z0-9-]*:[A-Za-z][A-Za-z0-9]*$` pattern on all four objects) admits only colon-prefixed keys, so the key form the spec names cannot validate on any of the four. The eleven-file example set contains no `Contract`, `Need`, `Award` or `Decision` instance, so the linkage has never been exercised and conformance rules 5 (chain closure) and 6 (no orphan hedged positions) have never resolved a position's `contract` reference to an in-tree instance | `docs/extensions/commodity-risk.md` §5, §10; `schema/{contract,need,award,decision}.schema.json` `propertyPaths` (no `commodityRisk`), `patternProperties`; `examples/commodity-risk/` (11 files, manifest) | Open — remedy `CP-Commodity-Risk-Linkage` §2.1 |
| D-46 | §6 of the same spec states ten new event types are added (`position.ingested`, `position.revalued`, `coverage.assessed`, `scenario.run`, `policy.breached`, `proposal.raised`, `proposal.approved`, `proposal.executed`, `mark.observed`, `snapshot.published`). `codelists/eventType.csv` (30 codes) carries none; D4 ("assessments are events") and `snapshot.published` (the content-hashed portfolio state) therefore have no minted vocabulary. The auction extension's event types sit in the core list bare (`auction.*`, `bid.*`), while CP-EventType-Closure §2.4 proposes prefixed extension values in an extension-owned list; the extension's events were specified against neither form. Seven of the ten names also depart from the minting convention CP-EventType-Closure §2.5 records (`object.verb`, lowerCamel object, past-tense verb) | `docs/extensions/commodity-risk.md` §6; `codelists/eventType.csv` (grep `snapshot.published` → 0 codelist hits); `governance/proposals/CP-EventType-Closure.md` §2.4, §2.5 | Open — remedy `CP-Commodity-Risk-Linkage` §2.2. Extends D-6 |
| D-47 | `codelists/policyEvaluationStatus.csv` exists (four rows, dated 5 July) and is absent from `codelists/bindings.json`. The inventory therefore reads `closure: unknown`, no closure marker, while spec §8 lists the codelist as **closed** and its four values are the ones the schema carries as a hand-maintained `enum` at `coverage-assessment.schema.json#/properties/policyEvaluation/properties/status`. Because the location is unbound, `check-codelist-binding.js` never asserts agreement between the CSV and the enum, and under IAR-0003 the enum would not be generated from it. A list the spec calls closed is, on the tree, a file nothing reads and an enum nothing checks — the D-1 pattern with the authoritative copy present but disconnected | `codelists/policyEvaluationStatus.csv` (manifest); `codelists/bindings.json` (no entry); inventory `codelists.files[path=codelists/policyEvaluationStatus.csv].closure` = `unknown`; `docs/extensions/commodity-risk.md` §8 | Open — remedy `CP-Commodity-Risk-Linkage` §2.3 |
| D-48 | §4.2 states `CoveragePolicy` is "a subtype of the core `Policy` (`policyType: coverageCorridor`)" — design principle D3, which the first review called "the property that makes it a SIGNET extension". `codelists/policyType.csv` carries five values and no `coverageCorridor`; `coverage-policy.schema.json` constrains `policyType` by nothing (no `enum`, no `const`, no `$ref`); `CoveragePolicy` shares no schema with `Policy` (no `$ref`, no `allOf`) and lacks `expressionLanguage`, `expression`, `version` and `issuedBy`, four of `Policy`'s seven required fields. The subtype relation exists in prose only; a `Mandate` bounded by a `Policy` reference cannot be bounded by a `CoveragePolicy` under any rule in the tree | `docs/extensions/commodity-risk.md` §4.2; `codelists/policyType.csv` `codes`; `schema/coverage-policy.schema.json` `required`, `refs`, `enums`; `schema/policy.schema.json` `required` | Open — remedy `CP-Commodity-Risk-Linkage` §2.4 |
| D-49 | `HedgeProposal` is declared lifecycle-bearing in `state-model/state-model.json` (`lifecycle: true`, `stateField: status`) with no states and no transitions; IAR-0004 records it as "not yet modelled". The seven-status lifecycle §4.6 specifies (`draft → pendingApproval → approved → executing → executed \| rejected \| withdrawn`) exists only as a hand-maintained schema `enum` — the surface IAR-0004 removed as the record for every lifecycle it declares. The one object that carries a risk decision into procurement (D1) has a lifecycle no transition has a `basis` for and no event can project | `state-model/state-model.json` `HedgeProposal`; `schema/hedge-proposal.schema.json` `enums` `/properties/status`; `governance/IAR-0004-registry-normative.md` "The change" | Open — remedy `CP-Commodity-Risk-Linkage` §2.5. Same shape as `OnboardingCase` and `SupplierQualification`, which this row does not cover |
| D-50 | Unit of measure is represented three ways and bound nowhere. `definitions.schema.json#/definitions/Unit` ("UN/ECE Rec 20") is referenced by zero `$ref` sites — a dangling definition. `InvoiceLine.unitOfMeasure` is a bare string with a Rec 20 `$comment`. The commodity-risk extension carries `unit` as an unconstrained string at seven sites (`volume.unit`, `deliveryPeriod.profile.unit`, `economics.{price,cost,regulatedPrice,tcoPrice}.unit`, `incrementalVolume.unit`, `PriceMark.value.unit`) and every example writes `MWh`, which is not a Rec 20 code (`MWH`). `Item.quantity` — the line-item quantity on `SourcingEvent`, `Submission`, `Order` and `Catalogue` — is a bare number with no unit at all. A 100 MWh order and a 100-unit order are the same instance | `schema/definitions.schema.json` (`Unit`: 0 refs across `schemas.files[*].refs`; `Item`: `quantity` only; `InvoiceLine.unitOfMeasure`); `schema/exposure-position.schema.json`, `hedge-proposal.schema.json`, `price-mark.schema.json` `propertyPaths`; grep `MWh` (26 hits, all extension), `MWH` (0) | Open — remedy `CP-Quantity-Unit` §2.1–2.2. First recorded as a KTDDE crosswalk gap; the shipped extension is the second consumer |
| D-51 | Delivery period is represented two ways with no shared primitive. Core `Order.deliveryPeriod` is a `Period` (start, end, duration). The extension's `deliveryPeriod` is `{year, basis, profile{unit, values[{period, value}]}}` on `ExposurePosition`, and `{year, basis}` on `HedgeProposal`, `PriceMark` and `CoverageAssessment` — four hand-maintained copies of one shape, none in `definitions`. `Contract`, the object D2 makes the source of every position, has no delivery surface at all: no items, no profile, no period other than the contract term. Nothing anywhere names a delivery point. A shaped-load supply contract cannot state what it delivers, where, or when, in core | `schema/order.schema.json` `deliveryPeriod` → `Period`; `schema/contract.schema.json` `propertyPaths` (11 entries, no delivery surface); `schema/{exposure-position,hedge-proposal,price-mark,coverage-assessment}.schema.json` `propertyPaths` `deliveryPeriod.*`; grep `deliveryPoint` → 0 | Open — remedy `CP-Quantity-Unit` §2.3. First recorded as a KTDDE crosswalk gap ("no delivery-terms primitive") |
| D-52 | `market` is an unconstrained string on five extension schemas and the examples write `DE`. The dimension the reconciliation rule (§9.1) partitions on, and the one a `CoveragePolicy.marketClassifications[]` entry keys on, has no identifier scheme. `codelists/identifierScheme.csv` (six rows) has no entry for the Energy Identification Code, the ENTSO-E scheme that identifies market areas, metering points and market participants, so no `Party`, position or policy in the tree can carry one even where one exists | `schema/{exposure-position,coverage-assessment,hedge-proposal,price-mark,coverage-policy}.schema.json` (`market` with no `enum`/`$ref`); `codelists/identifierScheme.csv` `codes`; grep `Energy Identification Code` → 0, `eic` → 0 | Open — remedy `CP-Market-Identifiers` §2 |
| D-53 | `CHANGELOG.md` v0.16.0 ("Terminal reachability. Check C11 …"), D-15 ("CI check C12") and D-16 ("CI check C14") cite CI checks in `conformance/rules/check-state-model.js` that the file does not implement: it declares "Ten checks" and carries C0–C10. C11–C14 exist in prose only — `CHANGELOG.md`, this register — and in no executable file. The registry changes those records describe (29 declarations, `basisScope`, corroboration) did land; the checks that keep them true did not. Two closures rest on guards that were never written — the D-31 / D-40 / D-44 family, found while preparing the response that closes D-40 | `conformance/rules/check-state-model.js` line 5 and its check ids; grep `E('C11'` … `E('C14'` → 0 in any `*.js` at `584a01c`; `CHANGELOG.md` v0.16.0; D-15, D-16 | **Closed** — C11–C14 landed in this change; D-15 and D-16 re-stated below |
| D-54 | `validate.js` validates examples through a hardcoded 25-entry `map` literal (`validate.js:33-58`). A CDM instance added to the tree and not added to that literal is checked against no schema, silently and with a green run. Eleven typed instance files live outside the map — five under `conformance/fixtures/endorsement/`, three under `agent/`, two under `onboarding/`, one under `auction/` — plus seven `Event` objects nested in `conformance/fixtures/endorsement/event-streams.json`. CI's only other pass over examples checks JSON well-formedness, and globs `examples/*.json`, which does not recurse into `examples/commodity-risk/`. The defect is the absent control, not any instance | `validate.js:33-58` (the `map` literal); `.github/workflows/validate.yml:22-23` and `:38-43` | Open — D-19 shape. Recorded on discovery while sourcing assessment materials; D-55 is an instance the absent control let through |
| D-55 | `onboarding/policy-onboarding.json` declares `"type": "Policy"` and does not validate against `schema/policy.schema.json`: three required properties absent (`expressionLanguage`, `version`, `issuedBy`) and one property present that the schema forbids (`appliesTo`; the schema lists nine properties under `additionalProperties: false`). `conformance/rules/check-onboarding.js` loads the file and does not schema-validate it. The onboarding extension's reference Policy instance is therefore not a conformant `Policy` | `onboarding/policy-onboarding.json`; `schema/policy.schema.json` `required`/`properties`; `conformance/rules/check-onboarding.js` (no AJV) | **Closed** — surfaced by D-54; four AJV errors reproduced against `main` at 273850d, then fixed on the instance side: the three required properties added (`expressionLanguage: rego`, matching the file's own `package signet.onboarding` expression) and `appliesTo` removed. No schema was changed — a normative change does not belong in a Tier 1 coverage record. The removal is D-57 |
| D-56 | `examples/approval.json` cites `underMandate` `did:web:buyer.example#mandate-doa-band4`; no instance carrying that id exists anywhere in the tree. The id occurs three times, at `examples/approval.json:8`, `agent/agent-runtime.js:129` (which emits the reference) and `wiki/Worked-Examples.md:172` — every occurrence a reference, none a definition. The Approval's authority leg is unresolvable. By contrast `examples/award-decision.json`'s `underMandate` (`mandate-eval-3`) does resolve, to `agent/mandate.json`, which is outside the validated example set (D-54) | `examples/approval.json:8`; `git grep mandate-doa-band4` — three hits, no instance | Open — distinct from D-8, which concerns effectiveness of a mandate that exists |
| D-57 | An onboarding eligibility policy has no declared field for the category it scopes to. `onboarding/policy-onboarding.json` carried the CPV classification as `appliesTo`, a key core `Policy` does not declare (nine properties under `additionalProperties: false`) and the onboarding extension does not declare either — `docs/extensions/onboarding.md` documents no such field. The value was shaped as a `Classification` and flowed through `onboarding/onboarding-runtime.js` into `SupplierQualification.classifications`. Removed under D-55, so the category the policy governs is now expressed in the runtime and in prose, and nowhere in the policy artifact a buyer would publish | `docs/extensions/onboarding.md` (no `appliesTo`); `schema/policy.schema.json` `properties`; `onboarding/onboarding-runtime.js` `CATEGORY`; `tools/inventory/CLAIMS.example.json` asserts "Policy has no property appliesTo" | Open — extension gap. Distinct from `CP-Policy-Applicability`, which proposes `Policy.appliesTo` with a different meaning (`appliesTo.transitions`: where a policy is *required*), and from the withdrawn `Policy.appliesTo` recorded at `governance/WITHDRAWAL-2026-08.md:20` |

| D-58 | `SourcingEvent` has no held or suspended state. `status` enumerates `planned, active, evaluating, complete, cancelled, withdrawn`. An event on hold has not ended (`cancelled`, `withdrawn`) and is not running (`active`), so a routine operational condition is either recorded as something it is not or not recorded at all. Practitioners report holds are common | `schema/sourcing-event.schema.json` `properties/status/enum` (six values); `state-model/state-model.json` (no hold state, no edge into or out of one) | Open — normative (schema and state model), Committee. Origin EAE-0001 #5. Remedy sketch: add `held` (or `suspended`) with `active↔held` transitions. Reversal risk: low to add, since no instance loses validity; removing a state instances already use is breaking. Design with D-59 — a held event that never resumes is often what gets reissued |
| D-59 | Reissuance has no lineage. No field links a reissued `SourcingEvent` to the event it replaces. The append-only grain answers reissuance well — a new event citing the old, never a resurrection — but the citation has nowhere to live, so the relationship is carried in identifiers, titles or nothing | 29 schemas — a case-insensitive grep over `schema/` for `supersede`, `replaces` or `predecessor` returns no hit; `schema/definitions.schema.json` offers no relation type to reuse | Open — normative, Committee. Origin EAE-0001 #5. Remedy sketch: `supersedes` (`Identifier`). Distinct from D-10, which asks the general supersession question across all 29 schemas and whose answer decides subject identity; this row records the specific `SourcingEvent` case the enquiry evidences. Design with D-58 |
| D-60 | The `procedure` codelist conflates legal award procedure with event kind, and omits market engagement. `codelists/procedure.csv` has five entries (`open`, `restricted`, `competitiveFlexible`, `directAward`, `frameworkCallOff`), framed on the UK Procurement Act 2023. Three problems in one field. (1) **Missing kinds:** RFI, RFC and invitation mechanics beyond `restricted` have no code; direct award exists, direct *order* — a PO raised without a sourcing exercise — arguably does not. (2) **Axis question:** RFI and RFC are market engagement, not award procedures, so coding them as `procedure` may be a category error; whether they are new `procedure` codes, a `SourcingEvent` kind, or event-level types is a gate for the remedying CP, not an assumption for this row. (3) **Jurisdiction lean:** the list's framing is one statute's, which is CP-Extension-Composition's core-versus-jurisdiction question arriving on a codelist | `codelists/procedure.csv` (five rows); `codelists/bindings.json` — `procedure` **is** bound, to `procedure.csv` at pointer `/properties/procedure`; `schema/sourcing-event.schema.json` `properties/procedure` is `{"type": "string"}` with the CSV named only in a description | Open — normative, Committee. Origin EAE-0001 #4. **Residual resolved at registration (10 Sep 2026):** `procedure.csv` **is** among `check-codelist-binding.js`'s four advisory failures, and the four are exactly D-39's four foreclosed names — `procedure`, `decisionType`, `partyRole`, `documentType`. So `procedure` is unenforced today (D-14) and its enforcement shape is undecided (D-39); this row adds that its *contents* are wrong independently of both. Cross-ref D-39: the remedy must use whatever extension-space pattern D-39's resolution adopts |
| D-61 | `Evaluation` admits one evaluator and `Score` forecloses more. `evaluation.schema.json` requires a single top-level `evaluatedBy` (`Identifier`); `scores[]` items are `Score`, whose four properties are `criterion`, `value`, `weight`, `rationale` under `additionalProperties: false` — no evaluator, and the closure forbids adding one inline. Per-criterion evaluation by different units of the buyer organisation is therefore inexpressible, and multi-evaluator reality is flattened into a single attribution that is false. This is an accountability defect, not a convenience one: the trail names an evaluator who did not make most of the determinations | `schema/evaluation.schema.json` `required` (includes `evaluatedBy`) and `properties/scores`; `schema/definitions.schema.json` `definitions/Score` — four properties, `additionalProperties: false` | Open — normative, Committee. Origin EAE-0001 #3a. The closure means any remedy is a schema change, not an extension, so it is squarely the Committee's and cannot be resolved under the Part 1 pattern. Cross-ref SE-0002 Q2: org-unit granularity decides what an evaluator reference can point at. Cross-ref D-5's concern — a status carrying something no event can project — in its accountability rather than its state-model form |
| D-62 | The deterministic close is defined over "the same rules over the same bids", but the order of placements, supersession, withdrawal, rejection, round boundaries and eligibility at close can each change the result, and none of them is an input to the definition. Two operators holding the same set of `Bid` objects with different event histories can both claim conformance and award differently | `docs/extensions/auction.md` §1 ("any conformant operator running the same rules over the same bids MUST reach the same close"), §5 closing paragraph, §9 ("close an identical bid set identically"); `schema/auction.schema.json` top-level `description`; `conformance/rules/check-auction.js:19-21` (A2 recomputes from materialised `Bid` objects with `status` `active` or `winning`, not from any event history) | Open – normative, Committee. Origin EAE-0002 P0-1. Remedy: `CP-Auction-Determinism` (to be registered under IAR-0006) |
| D-63 | Reverse-auction step, round completion and no-improvement close are under-specified for independent implementation. `minStep` binds "successive bids" without saying whose: a bidder's own previous offer or the current best. Nothing defines when a round completes, so a `no-improvement` close, which depends on a round completing without improvement, cannot be computed from the record. `Bid.round` is a bare integer and no event marks a round boundary | `schema/auction.schema.json` `rules.minStep` `description` ("Minimum decrement (reverse) or increment (forward) between successive bids"); `docs/extensions/auction.md` §3.2; `schema/bid.schema.json` `round`; `codelists/eventType.csv` (no `round.*` code) | Open – normative, Committee. Origin EAE-0002 P0-2. Remedy: `CP-Auction-Determinism` |
| D-64 | `tieBreak: earliest-bid` has no authoritative order to apply. `Event` carries no sequence or ordinal field; `timestamp` is the only time field and is wall-clock. The reference demonstration stamps every event with one constant timestamp, so "earliest" is undecidable on its own record. The checker resolves equal amounts by the order `fs.readdirSync` lists the dataset's files, which is a property of the filesystem, not of the rules | `schema/event.schema.json` `propertyPaths` (`id, eventType, subject, actor, timestamp, payload, previousEventHash, provenance`); `auction/auction-runtime.js:17` and `:41` (`timestamp: TS` on every event); `conformance/rules/check-auction.js:10` and `:20` (strict comparison in `reduce` keeps the first file listed on a tie) | Open – normative, Committee. Origin EAE-0002 P0-3. Design with D-71: chain position cannot replace the timestamp until chain scope is defined. Remedy: `CP-Auction-Determinism` |
| D-65 | `tieBreak: random-seeded` requires a published seed that has nowhere to live. `rules` is `additionalProperties: false` and declares no seed property, so a conformant `Auction` cannot carry one; any seed is necessarily outside the object whose rules it decides | `docs/extensions/auction.md` §3.2 ("MUST use a published seed"); `schema/auction.schema.json` `rules` (`additionalProperties: false`; properties `roundStructure, maxRounds, startPrice, reservePrice, minStep, closeCondition, tieBreak, identityDisclosure`) and `rules.tieBreak` `description` | Open – normative, Committee. Origin EAE-0002 P0-3. Remedy: `CP-Auction-Determinism` |
| D-66 | `tieBreak: split` contradicts the single-winner model. A split awards more than one bidder, but check A1 requires exactly one winning `Bid` per closed `Auction`, and `Award.awardedParty` is a single `Identifier`. No conformant record of a split close exists | `schema/auction.schema.json` `rules.tieBreak` `enum`; `conformance/rules/check-auction.js:17` (A1); `schema/award.schema.json` `awardedParty` (`$ref` `Identifier`) | Open – normative, Committee. Origin EAE-0002 P0-3. Remedy: `CP-Auction-Determinism` (reserve `split` until a multi-winner award model is designed) |
| D-67 | Auction amounts have no precision or arithmetic rule. `startPrice`, `reservePrice`, `minStep` and `Bid.value` are `Value`, whose `amount` is a JSON number. Nothing constrains fractional digits to the currency's minor unit or requires exact arithmetic, and the checker compares amounts as binary floating point. Nothing requires the amounts in one auction to share a currency, and the checker does not read `currency` | `schema/definitions.schema.json` `definitions/Value/properties/amount` (`type: number`); `conformance/rules/check-auction.js:20` and `:24` (numeric comparison of `value.amount`; `currency` not read); `docs/extensions/auction.md` (no precision text) | Open – normative, Committee. Origin EAE-0002 P0-4. Core `Value.amount` is not the remedy's target. Remedy: `CP-Auction-Determinism` |
| D-68 | The auction checker tests fewer properties than the extension and profile assert. `check-auction.js` implements A1–A3 and reads no `minStep`, `closeCondition`, `tieBreak`, `roundStructure`, `identityDisclosure`, `eligibility`, `currency` or event history. AP-2 says the close is verified "by replay (re-running the recorded bid sequence reproduces the identical award)"; no replay exists in the suite, and no auction fixture exists under `conformance/fixtures/`. Claim triad: the deterministic close is *modelled* and partly *tested*, which the profile's wording does not convey | `conformance/rules/check-auction.js` (rules A1–A3; none of the listed fields occurs in the file); `docs/profiles/auction-platform.md` §2 AP-2; `docs/extensions/auction.md` §9; `conformance/fixtures/` (no auction entry) | Open – conformance, Committee. Origin EAE-0002 P0-5. Remedy: `CP-Auction-Determinism` |
| D-69 | The schema admits rule combinations whose semantics are incomplete or contradictory. (1) **Dutch:** the extension says Dutch awards the highest bid, the checker treats Dutch as lower-wins, and a descending clock closed by first acceptance is not a standing-bid close under either reading. (2) **Direction:** `sealed-bid` and `multi-criteria` carry no direction; the checker infers "higher wins" for every type other than `reverse` and `dutch`, so a sealed-bid procurement is checked as if the highest price won. (3) **Lots:** `Auction.lots` is an array; `Bid` has no lot property, so a bid in a multi-lot auction cannot say which lot it prices. (4) **Award linkage:** `Award.sourcingEvent` is required and `Auction.sourcingEvent` is optional, so a closed `Auction` without one cannot construct a valid `Award` from its own record | `docs/extensions/auction.md` §5 ("english/dutch → the highest"); `conformance/rules/check-auction.js:18` (`lowerWins` for `reverse` or `dutch`); `schema/auction.schema.json` `lots`, `required`; `schema/bid.schema.json` `propertyPaths` (no lot); `schema/award.schema.json` `required` (includes `sourcingEvent`) | Open – normative, Committee. Origin EAE-0002 P0-6, P1-1, P1-2, P1-4, P1-8. Remedy: `CP-Auction-Determinism` (a machine-readable support matrix; unsupported combinations reserved) |
| D-70 | The event hash is defined only in code. `previousEventHash` is the output of `eventHash` in `conformance/runner/lib.js`: SHA-256 over a recursive sorted-key `JSON.stringify` of the whole prior event, including its own `previousEventHash`, rendered as `sha256:` followed by lowercase hex. No normative text names the algorithm, the canonicalisation or the output format: the specification says "Hash of the prior event", and the adapter contract says only "using the canonical hash defined in `runner/lib.js`". No tracked file names RFC 8785. An implementer in another language must reverse-engineer a JavaScript function to produce a verifiable chain, and no test vector exists | `conformance/runner/lib.js:79` (`stableStringify`), `:86` (`eventHash`); `docs/specification.md` `Event` table, `previousEventHash` row; `conformance/adapter/adapter-contract.md:43-44`; grep `8785` → 0 | Open – normative, Committee. Found verifying EAE-0002 P1-7. The sorted-key serialisation coincides with RFC 8785 for ordinary JSON values, but that is unstated and untested. Remedy: `CP-Auction-Determinism` §G names the algorithm as it is implemented and publishes vectors; whether the definition belongs in the core trust layer rather than an extension is recorded for the Committee |
| D-71 | Chain scope is defined two ways. The adapter contract chains **per subject**: each event links to "the subject's most recent event", and the reference adapter keeps one log per subject. The auction runtime chains **one stream across subjects**: every event links to the previous event emitted, whatever its subject, so `Auction` and `Bid` events interleave in one chain. Under the adapter contract an Auction and each of its Bids are separate chains, and the relative order of events on different subjects is not recoverable from `previousEventHash`. Both conform to `verifyChain`, which checks one array and is silent on scope | `conformance/adapter/adapter-contract.md:25` and `:43`; `conformance/adapter/reference-adapter.js:42` (`log.get(subject)`); `auction/auction-runtime.js:38-43`; `conformance/runner/lib.js:93` (`verifyChain`) | Open – normative, Committee. Found verifying EAE-0002 P0-3. Cross-ref `CP-Amendments-Round-2` and `CP-Tenancy`, which both record "no global chain" as a premise. Design with D-64. Remedy: `CP-Auction-Determinism` must state the auction's chain scope; the core question is the Committee's |
| D-72 | `docs/extensions/auction.md` §4 listed `superseded` as a `Bid.status` value after IAR-0002 removed it from the schema and recorded supersession as the `bid.superseded` annotation carrying `supersededBy`. Prose drift from the schema and state model | `docs/extensions/auction.md` §4 at v0.17.1; `schema/bid.schema.json` `status` `enum`; `state-model/state-model.json` `Bid` `bid.supersede` (`kind: annotation`) | **Closed** – #69 (editorial). Origin EAE-0002 §5 |

**Not recorded here, stated so it is not inferred missing.** D-38 already records that the six commodity-risk root schemas lack the Part 1 pattern; that row stands and is not duplicated. `Order` having no order date and `Classification.scheme` having no codelist are gaps found on the same crosswalk; neither is a divergence between a record and the tree, so neither is a defect, and both wait for constitution (held in `CP-Quantity-Unit` §7).

## Provenance of D-9 to D-13

Raised by implementer enquiry, verified against v0.15.0 before recording. Recording a defect is
not adoption of any proposed remedy, and none of these was recorded on an implementer's assertion
alone.

## Evidence appended after recording

Recorded here rather than inside the table rows, so that no existing row is edited and the date an
item of evidence arrived stays visible. A defect's row is the fact; this section is what has since
been said about it from outside Concert.

**D-39 — EAE-0001 (2026-09-09).** Public bodies are "very sensitive about how each type [of
sourcing event] is handled and the rules around what is allowed to happen how." Per-type
procedural rules are a compliance requirement, not a convenience. The extension space D-39 records
as foreclosed — `procedure` is one of its four names — is therefore load-bearing in exactly the
deployments the standard targets, and the generator shape D-39 asks about decides whether it
exists. See also D-60, which records that the same codelist's *contents* are wrong independently
of how it is enforced.

**D-57 — cross-reference, second surface.** EAE-0001 #2 and `CP-Qualification-Lifecycle` gate QL-2:
category-scoped qualification status raises the same "where does the governed category live"
question in `SupplierQualification` that D-57 records for `Policy`. One design decision, two
surfaces; an answer for `Policy` that does not also answer `SupplierQualification` leaves the
second surface open.
